name: motivational_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2

  intl: ^0.18.1

  http: ^1.4.0
  get_storage: ^2.1.1
  shared_preferences: ^2.5.3
  share_plus: ^11.1.0
  image_picker: ^1.2.0
  url_launcher: ^6.3.2

  # -----------Authentication-----------
  google_sign_in: ^7.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/badges/
    - assets/icons/
